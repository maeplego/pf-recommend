# P07 pf-recommend

このディレクトリは製品コードです。実装前にワークスペース（`project`）側を読む。

1. `project/portfolio-plan/instructions.md` または `project/AGENTS.md`
2. `project/recommend/AGENTS.md`
3. `project/portfolio-plan/recommend/DESIGN.md`
4. `project/portfolio-idea/20-recommendation-engine.md`
5. `project/recommend/chat-context/` をファイル名順

テスト: リポジトリルートで `python -m pytest`（`pip install -e ".[dev,api,train]"`）。通ってからこのリポジトリにコミットする。巨大な npy / 学習成果物は Git に置かない。
