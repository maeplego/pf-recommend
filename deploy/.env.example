# Inference
RECOMMEND_HTTP_PORT=8098
RECOMMEND_MODELS_DIR=/models
RECOMMEND_CORS_ORIGIN=http://localhost:3008

# P10 later: TALENT sets RECOMMEND_API_URL=http://localhost:8098
# (or the overlay service URL). This repo does not call P10.
