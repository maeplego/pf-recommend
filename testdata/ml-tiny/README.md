# 架空の MovieLens 形データ

CI と Compose 用です。列は GroupLens の `ratings.csv` / `movies.csv` に合わせていますが、タイトルは創作です。公開 MovieLens そのものとして扱わないでください。

公開の `ml-latest-small` で学習するときはルート README の手順でダウンロードします。Git には置きません。
